apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notes-chart.fullname" . }}-test-connection"
  labels:
    {{- include "notes-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notes-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
